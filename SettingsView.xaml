<UserControl x:Class="FlowLauncherCommunity.Plugin.SavvyCalLinkList.SettingsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             DataContext="{Binding RelativeSource={RelativeSource Self}, Path=Settings}"
             mc:Ignorable="d" d:DesignHeight="300" d:DesignWidth="600">
    <Grid Margin="70, 15, 15, 15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="200" />
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Column="0" Text="API Token" />
        
        <PasswordBox Name="PasswordBox" Grid.Column="1" PasswordChanged="PasswordBox_OnPasswordChanged"  />
    </Grid>
</UserControl>

